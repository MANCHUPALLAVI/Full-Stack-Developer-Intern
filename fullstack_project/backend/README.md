# Backend — PDF Uploader
Run:

1. npm install
2. npm start

It expects backend at http://localhost:4000 by default.
s
Backend folder:
- index.js: Express app
- uploads/: where uploaded files are stored (created at runtime)
- data.db: SQLite DB (created at runtime)
